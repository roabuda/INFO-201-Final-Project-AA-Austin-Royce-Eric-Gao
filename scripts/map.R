#get joined data
library(dplyr)
library(plotly)
library(ggplot2)
joined <- read.csv("./data/joined.csv")

#create a function that takes in numbers for min and max energy, 
#a vector of what types of energy to compare, and shows each
#state's population and which party won in 2016

create.map <- function(min,max,energy)
{
  
}

